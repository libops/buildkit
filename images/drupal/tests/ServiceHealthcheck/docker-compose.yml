---
name: drupal-servicehealthcheck
services:
  drupal:
    image: ${DRUPAL:-libops/drupal:local-php83}
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost/status"]
      interval: 30s
      timeout: 5s
      retries: 20
      start_period: 10m
