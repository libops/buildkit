---
name: wp-servicehealthcheck
services:
  wp:
    image: ${WP:-libops/wp:local-php83}
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost/status"]
      interval: 30s
      timeout: 5s
      retries: 20
      start_period: 10m
